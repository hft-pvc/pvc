
public class Core {

/*	public static void main(String[] args) {
		
		String port = null;
		if (args.length > 0)
			port = args[0];
		
		if (port == null) {
			System.err.println("Needs port...");
			System.exit(1);
		}
		Connection con = new Connection(port);
		
		while (true) {
			con.read();
		}
	}*/
}
